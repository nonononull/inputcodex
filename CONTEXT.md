# inputcodex 项目语境

`inputcodex` 在保持上游有效功能行为一致的前提下，以纯 Rust 原生架构重新实现 Codex 桌面增强与管理能力。本文件只定义项目专用术语，不描述具体实现。

## 上游与基线

**上游稳定基线**：
`BigPizzaV3/CodexPlusPlus` 最新正式 Release，是功能一致性的唯一基准；当前活动基线为 `v1.2.44`、提交 `77091ccaee4423f35a1b2c51c4ecd703e6201092`。
避免用语：上游最新版、参考版本、原版

**上游预警源**：
`BigPizzaV3/CodexPlusPlus` 的 `main` 分支，只用于提前发现变化，不直接改变功能基线。
避免用语：开发基线、自动同步源

**上游快照**：
保存在 `inputcodex` 仓库内的完整上游源码副本，不包含上游 Git 历史；只能通过上游同步 PR 更新。
避免用语：Submodule、依赖源码、Vendor 依赖

**半成品参考**：
`zsr131550/CodexPlusPlus`，仅提供原生迁移、功能合同、测试与性能排错证据，不是功能真源或代码底座。
避免用语：下游基线、新架构基线

**审计专用上游资产**：
上游 Tauri/React 管理界面、现有注入脚本、远程推荐列表、广告代码和素材；它们随完整快照进入仓库，但不得作为新架构代码、运行时依赖或动态数据源。其背后的有效能力需要独立功能或一致性例外 Issue 才能重新设计。
避免用语：待迁移模块、运行时资源、兼容依赖

## 一致性与差异

**功能一致性**：
输入、输出、数据兼容、持久化结果、副作用、错误语义和操作流程与上游稳定基线一致；不要求 UI 像素一致。
避免用语：代码一致、界面复制、文件一致

**一致性例外**：
因功能无效、有害副作用、错误语义争议、广告、推广或遥测而不能直接保持一致的行为；必须由项目所有者在 Issue 中决定。
避免用语：顺手修复、合理优化、默认删除

**迁移项**：
把一个已登记的上游功能行为重新实现到 `inputcodex` 新架构中的有界交付单元。
避免用语：搬代码、同步功能

**受保护路径**：
只在进程内部用于文件系统或平台 API 的绝对路径值；不得序列化、公开显示或进入错误文本，自定义调试输出必须脱敏。
避免用语：可展示路径、日志路径、公共路径字符串

**平台路径快照**：
一次只读解析产生的 Codex 安装、`CODEX_HOME` 与 `inputcodex` 状态路径集合；不创建目录、不写文件、不缓存、不监听。
避免用语：路径配置、持久化路径、后台路径服务

**应用概览快照**：
一次只读采集产生的 Codex 安装事实、已安装版本或明确未知原因、`inputcodex` 构建版本、采集时间与实时进程观察状态；不包含快捷方式、更新、设置、诊断、历史启动记录或生命周期操作。
避免用语：管理首页数据、聚合 Payload、运行状态缓存

**版本未知**：
已确认 Codex 安装存在，但版本元数据缺失、不可读或非法；该状态不否定安装事实，也不扩大为整个应用概览失败。
避免用语：未安装、读取失败、空版本

**实时进程未观察**：
`LiveProcessState::NotObserved`，表示当前切片没有执行实时进程观察；不得从历史启动记录、旧 PID、debug port 或状态文件推断运行中或已停止。
避免用语：未运行、已停止、上次运行状态

**版本与启动快照**：
一次无副作用进程输入解析产生的 `inputcodex` 编译版本与启动意图；版本只来自 `CARGO_PKG_VERSION`，不检查远端版本，不读取文件，不执行更新。
避免用语：更新状态、版本检查结果、启动配置缓存

**启动意图**：
`StartupIntent::Default` 或 `StartupIntent::ShowUpdate`；只表达本次进程启动请求，不代表已经打开更新界面、执行更新检查、下载或安装。
避免用语：更新模式已启动、更新任务、后台更新状态

**运行时环境冲突观察**：
一次只读检查当前 `inputcodex` 进程实际继承的 `OPENAI_*` 环境变量名称和值存在状态；不返回原始值，不删除变量，不读取用户级或系统级持久化环境，也不写备份。
避免用语：环境清理、整机环境扫描、环境修复完成

**环境来源覆盖**：
运行时进程、用户级持久化环境和系统级持久化环境是否被本次请求真实观察的明确声明；当前切片固定为 `runtime_process: Observed`、`persistent_user: NotObserved`、`persistent_system: NotObserved`。
避免用语：无系统冲突、全部环境已检查、持久化环境为空

**Relay 环境只读观察**：
一次无副作用聚合当前进程与平台允许来源中的五个固定代理变量名称、`CODEX_HOME/.env` 存在状态和四个固定 Clash Verge 候选的 `enable_tun_mode` 状态；不测试网络，不返回变量值、文件内容、注册表值或实际路径。
避免用语：网络环境检测、代理修复、Relay 配置扫描

**局部观察不可用**：
某个持久化环境来源、`.env` 元数据或 Clash 候选无法读取，但其余来源仍形成可信快照；结果继续为 `Ready` 并保留 `Unavailable`、`Unreadable` 或 `Invalid`，不得静默当成不存在或扩大为整次失败。
避免用语：没有配置、无风险、读取成功

**Clash TUN 候选状态**：
四个固定逻辑来源分别记录 `Absent`、`Disabled`、`Enabled`、`Unreadable` 或 `Invalid`；重复实际路径只读取一次再映射回逻辑来源，任何状态都不得公开候选路径或配置内容。
避免用语：Clash 已开启、Clash 未安装、配置文件详情

**Relay 认证与配置状态观察**：
一次只读观察平台路径能力定位的固定 `auth.json` 与 `config.toml`，只返回文档状态、凭据存在事实和 Relay 配置完整性；不返回账号、Token、Provider、URL、字段、内容、认证来源或实际路径。
避免用语：登录详情、账号状态、Relay 配置内容

**Relay 文档状态**：
`Missing`、`Valid`、`Invalid`、`TooLarge` 或 `Unreadable`；单份文档问题不得静默冒充未认证或未配置，也不得覆盖另一份文档的可信事实。
避免用语：没有凭据、没有配置、读取成功

**Sub2API 账单倍率观察**：
使用受控 Relay profile 的 Base URL 与 API key 发起一次有界网络读取，只返回经过 schema、token scope 与非负有限值校验的倍率和观测时间；不接受独立任意 URL，不返回或持久化凭据、账号、Header、原始响应或实际端点。
避免用语：账单管理、费用结算、倍率已保存

**Watcher 偏好状态观察**：
一次只读检查平台路径能力定位的 `inputcodex_state_root/watcher.disabled` 元数据；标记缺失只表示 `EnabledByDefault`，普通文件存在只表示 `ExplicitlyDisabled`。它不读取文件内容、不返回实际路径，也不表示 Watcher 已安装、正在运行或已控制任何进程。
避免用语：Watcher 运行状态、Watcher 已启用、后台守护已安装

**Watcher 偏好变更**：
一次针对平台路径能力定位的固定 `watcher.disabled` 标记执行的 typed 变更；请求同时声明预期偏好和目标偏好，结果区分已应用、已经满足、冲突、取消、失败与提交后不确定。它只改变偏好标记，不安装、启动、停止或探测 Watcher。
避免用语：Watcher 启停、Watcher 已运行、通用文件写入

**Watcher 偏好变更收据**：
Watcher 偏好变更完成后必须交付的最小事实，包含请求标识、目标偏好、状态根 setup 提交、标记提交、最终偏好观察、结果分类和稳定诊断码；提交点后的取消不得使该收据失效或被当成过期结果丢弃。
避免用语：写入成功、取消成功、Watcher 状态

**设置文档观察**：
一次只读解析平台路径能力定位的设置文件，只保留 JSON object 顶层条目数量；不接受任意路径，不返回字段名称、字段值、原始内容、脚本或实际路径。
避免用语：设置详情、设置快照、配置内容

**设置未配置**：
设置文件不存在时的 `LoadCompletion::Empty` 语义；合法空对象 `{}` 必须是 `Ready` 且条目数量为 `0`，不得把两者合并为同一空结果。
避免用语：空设置、默认设置、没有任何配置

**诊断日志结构观察**：
一次只读观察平台路径能力定位的固定诊断日志文件，只保留文件字节数、尾部样本记录数、合法 JSON object 数、损坏记录数和截断事实；最多读取尾部 `256 KiB`，不接受任意路径。
避免用语：日志内容、诊断详情、日志导出

**诊断记录损坏**：
尾部样本中的空行、非法 UTF-8、损坏 JSON 或非 object JSON；它们作为 `malformed_record_count` 的成功观察事实保留，不升级为整文件失败，也不得通过 lossy 文本伪装成合法内容。
避免用语：已忽略、自动修复、无有效日志

**上下文能力只读目录观察**：
一次只读识别当前 Codex 配置声明的 MCP Server、Skill 和 Plugin，只保留条目 ID、稳定种类、启用状态和分类计数；不等同于查看、编辑、同步或执行其配置。
避免用语：上下文配置详情、插件配置快照、工具管理已完成

**上下文目录未配置**：
固定配置文档不存在时的 `LoadCompletion::Empty` 语义；合法配置存在但没有任何上下文条目时必须是 `Ready` 且条目数为 `0`，两者不得合并。
避免用语：空配置、没有工具、配置读取成功

**上下文条目启用状态**：
条目声明是否允许被后续选择流程使用的结构事实；它不表示对应 MCP、Skill 或 Plugin 已加载、可执行、健康或正在运行。
避免用语：工具已启动、插件可用、能力运行中

**本地会话清单快照**：
一次有界只读观察产生的本地会话条目、分页和来源覆盖事实；条目只包含会话标识、有界展示标题、归档状态和更新时间，不等同于读取对话正文、实时运行状态或会话管理能力。
避免用语：完整会话、会话内容、会话管理已完成

**会话来源覆盖**：
本地会话清单是否完整覆盖全部已发现来源的明确状态；`Complete` 表示全部来源成功观察，`Partial` 表示已返回部分条目但至少一个来源失败，不得把 `Partial` 表述为完整成功。
避免用语：全部会话、完整历史、失败已忽略

**受控会话 Markdown**：
由既有本地会话标识触发，经严格只读 SQLite 与受控 rollout 生成的内存 Markdown 文档；它只包含用户与助手文本及不可联网的图片占位，不表示文件已经保存。
避免用语：Markdown 文件、导出已保存、完整内部会话

**Markdown 建议文件名**：
受控会话 Markdown 随结果返回的有界、跨平台安全名称；它不含完整会话标识，也不是调用方可传入的输出路径。
避免用语：目标路径、已写入文件、原始会话名

**Local Storage 模型后缀清理例外**：
上游在 renderer 注入成功后通过 CDP 执行 JavaScript，读取并改写 Electron Local Storage 中历史 Token 数据的模型名，同时尝试写诊断日志；inputcodex 未批准迁移该副作用链，目录状态固定为 `exception-pending`。
避免用语：Token 历史只读观察、无注入规范化、自动安全清理

**rollout Token 历史候选**：
上游 `codex-plus-data` 从受控 SQLite 定位 rollout，再解析 `token_count` 事件形成逐轮用量历史的另一项能力；它不属于 `core-module:codex_local_storage`，必须独立评估会话标识、路径、资源上限和最小披露后才能迁移。
避免用语：已实现 Token 历史、Local Storage Token 读取、完整用量导出

**副作用准入矩阵**：
对当前 `unassessed` source 的治理投影；按 `write -> process -> network` 优先级记录能力桶、typed owner
状态、owner kind 与稳定 blocker ref。矩阵只证明来源仍被阻断，不授予实现权限，也不改变 feature/source
disposition 或产品完成度。
避免用语：实现队列、已批准候选、产品路线图

**已消费有限 tranche**：
已完成其全部批准批次且不得再次选择候选的自治授权记录。`consumed` 只终止该 tranche 的候选投影，
不会把剩余 Gate 5 来源标记为完成，也不会自动扩大到新的文件、网络、进程或凭据副作用。
避免用语：持续授权、自动续批、Gate 5 已完成

## 版本与发布

**上游基础版本**：
`inputcodex` 当前对齐的上游稳定版本，例如 `1.2.44`。
避免用语：本地版本、兼容版本

**inputcodex 修订号**：
在同一上游基础版本上的独立发布序号，例如 `inputcodex.1`、`inputcodex.2`。
避免用语：补丁号、内部构建号

**自主更新源**：
由 `nonononull/inputcodex` 提供的 Release、更新清单、签名和下载资产；客户端不得从上游仓库下载更新。
避免用语：上游更新源、镜像更新源
