schema_version: inputcodex.fixture-manifest.v1
feature_id: feature.session-data.token-usage-history
fixtures:
  - id: fixture.feature.session-data.token-usage-history.baseline
    scenario: baseline
    kind: synthetic
    files:
      - path: baseline.yml
        format: yaml
        description: '合成 Token 用量历史，覆盖输入与输出计数。'
