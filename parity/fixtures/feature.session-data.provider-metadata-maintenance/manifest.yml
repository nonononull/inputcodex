schema_version: inputcodex.fixture-manifest.v1
feature_id: feature.session-data.provider-metadata-maintenance
fixtures:
  - id: fixture.feature.session-data.provider-metadata-maintenance.baseline
    scenario: baseline
    kind: synthetic
    files:
      - path: baseline.yml
        format: yaml
        description: '合成 Provider metadata 同步与索引清理预览。'
