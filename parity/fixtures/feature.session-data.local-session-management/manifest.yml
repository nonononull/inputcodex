schema_version: inputcodex.fixture-manifest.v1
feature_id: feature.session-data.local-session-management
fixtures:
  - id: fixture.feature.session-data.local-session-management.baseline
    scenario: baseline
    kind: synthetic
    files:
      - path: baseline.yml
        format: yaml
        description: '合成 CODEX_SQLITE_HOME、双数据库删除、组合撤销、全库预检与撤销窗口，不含真实会话或私人绝对路径。'
