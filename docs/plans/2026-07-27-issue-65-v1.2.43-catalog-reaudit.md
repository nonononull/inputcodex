# Issue #65：`v1.2.43` 功能目录重新审计实施计划

## 任务元数据

- Issue：`https://github.com/nonononull/inputcodex/issues/65`
- 基线：`15e91708b41548f523e26ede4c7ca4de41badf77`
- 分支：`codex/issue-65-v1.2.43-catalog-reaudit`
- 上游正式 Release：`v1.2.43 @ 5036ff056b5c629f19356396b17d6eeb70da664c`
- 上游 tree：`d478a9fcda7f22a7c8167cb567777ad9148cf328`
- 已批准范围：二十四路径
- `scope_hash`：`sha256:82234e7aacce0bd6c57994529ccf74371052ed906dc8371324b90e41f697d7b7`
- 实施批准：`https://github.com/nonononull/inputcodex/issues/65#issuecomment-5087166207`
- 最终 Squash Merge：保留项目所有者对最终 PR Head 的单独授权门

## 目标

以 `BigPizzaV3/CodexPlusPlus v1.2.43` 为唯一功能真源，在不迁移上游运行面、不新增产品功能、不修改性能预算和 Gate 5 的前提下：

1. 将五域功能目录、五域行为合同与 `source-index` 的 Release 锚点推进到 `v1.2.43`。
2. 固定 Watcher 在 macOS 按 debug port 精确终止并等待旧 Codex 进程退出后再重启的语义。
3. 固定 Provider Sync 从 `threads` 修复缺失的本地 SQLite catalog 行，并公开 `sqliteCatalogRowsInserted` 计数的语义。
4. 记录用户脚本运行时状态与错误回传的新证据，但继续保持注入链 `exception-pending`。
5. 记录新增 sponsor、到期字段和素材的新证据，但继续保持广告与推广能力 `exception-pending`。
6. 在证据、测试与仓库政策全部通过后，将 `release_audit` 从 `stale-re-audit-required` 恢复为 `current`。

## 不变量

- 来源入口仍为 `133`，其中 Tauri/Core/Data 为 `84/45/4`；不新增或删除入口。
- 功能、合同和 fixture 数量保持 `36/36/11`，覆盖缺口保持 `0`。
- `exception-pending` 保持 `10`，排除项保持 `3`。
- 上游 Tauri/React、WebView、renderer/inject、Dream Skin runtime、远程推荐与广告素材只作为审计输入。
- 产品 Rust、Iced、Cargo、CI Workflow、Ruleset、性能预算和 Gate 5 均不得变化。

## 上游差异分类

`v1.2.42...v1.2.43` 共 `33` 个变化文件：`30` 个修改、`3` 个新增、`0` 个删除。

| 分类 | 结论 |
| --- | --- |
| Watcher 与 launcher | macOS 新增 launcher/Codex 进程终止、等待退出及按 debug port 精确筛选；更新受支持功能语义。 |
| Provider Sync | 新增从 `threads` 修复 `local_thread_catalog`、同步状态水位及 `sqliteCatalogRowsInserted`；更新合同和合成 fixture。 |
| User Scripts | 新增 renderer 运行时状态与错误合并；因仍依赖注入链，只补审计证据并维持例外。 |
| 广告与 sponsor | 新增 sponsor、到期时间和本地素材；受无广告硬规则阻断，只补例外证据。 |
| Tauri/React、Dream Skin、renderer/inject | 只作为完整快照输入，不进入新架构或最终运行面。 |
| Cargo/npm/版本、图片、上游 Superpowers 文档 | 不形成新的产品功能事实。 |

## TDD 顺序

1. **RED**：先把仓库级目录测试推进到 `v1.2.43`，新增 Watcher、Provider、User Scripts、广告例外和 Provider fixture 断言；确认旧目录按预期失败。
2. **GREEN**：仅修改已批准目录、合同、source-index、fixture 和 `source-lock`，使定向测试通过。
3. **REFACTOR**：只清理本任务引入的重复文本或命名，不改生产 Rust 和目录验证器。
4. **VERIFY**：执行本地轻量命令、范围审计、Git 快照与 GitHub-hosted CI。

## 精确二十四路径

```text
AGENTS.md
README.md
build.md
crates/inputcodex-parity/tests/catalog_repository.rs
docs/plans/2026-07-27-issue-65-v1.2.43-catalog-reaudit.md
docs/plans/PROJECT-MASTER-PLAN.md
docs/plans/sessions/2026-07-27-issue-65-v1.2.43-catalog-reaudit.md
docs/reports/issue-65-v1.2.43-catalog-reaudit-discovery.md
docs/workflows/2026-07-27-issue-65-v1.2.43-catalog-reaudit-runtime.md
parity/README.md
parity/contracts/foundation-platform.yml
parity/contracts/plugin-script.yml
parity/contracts/provider-network.yml
parity/contracts/remote-install.yml
parity/contracts/session-data.yml
parity/features/foundation-platform.yml
parity/features/plugin-script.yml
parity/features/provider-network.yml
parity/features/remote-install.yml
parity/features/session-data.yml
parity/features/source-index.yml
parity/fixtures/feature.session-data.provider-metadata-maintenance/baseline.yml
parity/fixtures/feature.session-data.provider-metadata-maintenance/manifest.yml
upstream/source-lock.json
```

## 本地验证

```powershell
cargo test -p inputcodex-parity --test catalog_repository --offline
pwsh -NoProfile -File scripts/ci/Test-CiScripts.ps1
pwsh -NoProfile -File scripts/ci/Verify-RepositoryPolicy.ps1 -RepositoryRoot .
git diff --check
```

完整 Workspace、Windows/macOS/Linux 编译测试和最终 Release Audit 由标准 GitHub-hosted CI 执行。

## 停止门

- 二十四路径集合或 `scope_hash` 变化。
- 上游正式 Release、commit、tree 或 33 文件集合漂移。
- 需要修改产品 Rust、Cargo、Workflow、Ruleset、预算、Gate 5、UI 或 AGOS。
- 发现功能存在无效语义、有害副作用或需要改变现有例外决定。
- 本地验证、Review 对话或最终 PR Head CI 失败且根因未解决。
- 未取得项目所有者对最终 Head 的单独 Squash Merge 授权。

## 当前状态

项目所有者已批准方案 A、二十四路径和精确 `scope_hash`；允许 TDD 实施、轻量验证、普通提交与推送、非 Draft PR、Review/CI。

RED 已于本机时间 `2026-07-27 12:21:43 +08:00` 建立：目录测试输出 `9 passed; 3 failed`，失败只来自五域与 `source-index` 仍为 `v1.2.42`、六处 Release 不匹配以及 Watcher 等 `v1.2.43` 新语义尚未落入目录。最终 Squash Merge 尚未授权。
