# Issue #115：`v1.2.44` 功能目录重新审计实施计划

## 任务元数据

- Issue：`https://github.com/nonononull/inputcodex/issues/115`
- standing authorization：`https://github.com/nonononull/inputcodex/issues/111`
- Planning Freeze：`https://github.com/nonononull/inputcodex/issues/115#issuecomment-5150369262`
- 基线：`057c7e08de5c1e30198881a7f548a49453fb1ac6`
- 分支：`codex/issue-115-v1-2-44-catalog-reaudit`
- 上游正式 Release：`v1.2.44 @ 77091ccaee4423f35a1b2c51c4ecd703e6201092`
- 上游 tree：`d417f2775adfe61798c6676024edce1a236e576f`
- 候选范围：`32` 路径
- `scope_hash`：`sha256:b8d42285dc7cfca080f9fbf683c9c8176a0faae633c5971b1827837059898b83`

## 目标

以 `BigPizzaV3/CodexPlusPlus v1.2.44` 为唯一功能真源，在不修改上游快照、不迁移产品代码的前提下：

1. 将五域功能目录、行为合同和 source-index 对齐 `v1.2.44`。
2. 登记新增 `core-module:sub2api` 与 `tauri-command:fetch_sub2api_billing`。
3. 固定 provider 导入安全、凭据隔离、Responses compact/zstd/WebSocket、模型 capability、launcher/CDP/data bridge、插件 fallback 与 `CODEX_HOME` 行为。
4. 使用合成 fixture 固定结构事实，不保存真实凭据、URL、账号、路径或响应正文。
5. 证据闭环后将 `release_audit` 从 `stale-re-audit-required` 恢复为 `current`。

## 架构决定

选择新增 `feature.provider-network.sub2api-billing-observation`：该能力有独立的凭据网络读取、schema/token scope 校验、超时、取消、错误隔离和最小披露边界。

- 不归入 `provider-diagnostics`：账单倍率不是连接测试或 Provider Doctor，合并会混淆输出和错误语义。
- 不归入 `relay-profile-management`：读取倍率本身不写配置，合并会给现有文件读写合同隐式增加网络副作用。
- 初始状态保持 `unassessed`：本任务只登记审计事实，不宣称已在 inputcodex 产品运行面实现。

## 不变量

- 来源入口固定为 `135`：Tauri/Core/Data 为 `85/46/4`。
- feature/contract/fixture 固定为 `44/44/13`，覆盖缺口为 `0`。
- `exception-pending=10`、显式排除 `3`；新增 Sub2API 为普通 `unassessed`。
- 产品 Rust、Cargo、性能预算、Workflow、Ruleset、README、err、上游快照和 AGOS 均不得变化。
- Tauri/React、renderer/inject、广告、远程推荐和 UI 仅为审计输入。

## 变化分类

| 分类 | 目录处理 |
| --- | --- |
| Sub2API 倍率 | 新增独立 feature、contract、两条 source 与合成 fixture。 |
| Provider 导入与 common config | 更新 provider-import、relay-profile-management 及脱敏 fixture。 |
| Web Search / Responses Lite | 更新 model-catalog 与 capability fixture。 |
| Responses compact / zstd / WebSocket | 更新 protocol-proxy；不新增产品实现。 |
| mixed relay / profile switching | 更新 provider-configuration-application。 |
| launcher / CDP / data bridge | 更新 application-lifecycle；注入部分继续例外。 |
| provider sync 的 CODEX_HOME | 更新 provider-metadata-maintenance 与 fixture。 |
| 插件 fallback / Quick Chat / Fast / companion | 更新既有 plugin-script 例外，不改变状态。 |
| sponsor、Tauri/React、样式、图片 | 只保留广告/视觉审计证据，不进入运行面。 |

## TDD 顺序

1. **RED**：先将目录测试推进到 `v1.2.44`、`135/44/44/13` 和新行为断言。
2. **GREEN**：只修改冻结范围内的目录、合同、fixture、文档与 `release_audit`。
3. **REFACTOR**：仅清理本任务引入的重复描述，不修改验证器或产品代码。
4. **VERIFY**：运行 `build.md` 的 Issue #115 本地轻量门，再进入普通 push、非 Draft PR、独立复审和 Hosted CI。

RED 已在提交 `cce1224e0f0ad83de0ab37feb77fc02d3f4b4a4b` 复现：`24 passed / 4 failed`，失败只来自旧 Release、旧计数、缺失两条 Sub2API source 和缺失 v1.2.44 行为证据。

GREEN 已于 Windows 本机时间 `2026-08-01 15:34:54.751 +08:00` 通过：目录 `28/28`、CI 合同 `76/76`、Release Audit `current`、Repository Policy `ok=true / 0`、rustfmt 与 `git diff --check` 全部通过；实际范围精确为 32 路径和冻结 hash。

## 精确范围

完整 32 路径清单和 Ordinal hash 复算命令位于 `build.md` 与 Runtime Workflow。根 `README.md` 和 `err.md` 不在范围内；只有出现新的可复用纠错根因才允许重新冻结范围。

## 验证

```powershell
cargo test -p inputcodex-parity --test catalog_repository --offline
pwsh -NoProfile -File scripts/ci/Test-CiScripts.ps1
pwsh -NoProfile -File scripts/ci/Verify-ReleaseAuditGate.ps1 -RepositoryRoot .
pwsh -NoProfile -File scripts/ci/Verify-RepositoryPolicy.ps1 -RepositoryRoot .
git diff --check
```

## 停止门

- 32 路径、scope hash、Release commit/tree 或 42 文件集合漂移。
- 需要修改产品 Rust/Cargo、Workflow、Ruleset、上游快照、README、err、UI 或 AGOS。
- Sub2API 需要任意 URL、凭据回显/持久化，或无法保持双平台同一安全语义。
- Review 对话、Final Head CI/Performance、Artifact 0、origin/main freshness 或精确 Head 证据不成立。

现有自治解析器没有 re-audit task kind，会在合法 stale 输入上报告 `RELEASE_AUDIT_STALE`。本任务不越界修复控制面，使用项目原生流程与 #111 standing authorization 继续；最终合并仍必须满足精确 Final Head 的全部机器门禁。
