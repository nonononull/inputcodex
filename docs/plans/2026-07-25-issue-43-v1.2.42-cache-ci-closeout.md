# Issue #43：`v1.2.42` 缓存与 CI 合同状态收口计划

## 任务元数据

- Issue：`https://github.com/nonononull/inputcodex/issues/43`
- 基线：`353391424db5514d022473ba97f601486a190869`（`origin/main`）
- 实现分支：`codex/issue-43-v1.2.42-cache-ci-closeout`
- 本机计划时间：`2026-07-25 15:14:12 +08:00`，来自 Windows `Get-Date`
- 所有者范围授权：`user-message:批准-Issue-43-十路径与-scope_hash-允许实施轻量验证提交推送PRReviewCI-2026-07-25`
- 精确写入范围状态：`已批准`
- 精确范围批准：Issue `#43` 的 Discovery 评论与项目所有者后续明确授权
- `scope_hash`：`sha256:39a26c9301f8ca792498be73117320b1d26c3028fdc1c558703ee7cefb96061e`
- 最终 Squash Merge：必须在最终 PR Head 的 Review、CI 和所有对话闭环后，取得项目所有者单独授权。

## 收口目标

1. 将长期控制面从已过期的 Issue `#35` / PR `#36` 进行中描述，更新为已完成的 `#35/#36`、`#34/#40` 与 `#41/#42` 稳定事实。
2. 固定 `v1.2.42` 只读缓存、`stale-re-audit-required` 的合法语义和 Issue `#38` 的独立重新审计边界。
3. 记录 PR `#40` 基线分支推进未自动触发新 `pull_request` Run 的根因，以及仅关闭并立即重新打开 PR 的受限恢复与验证事实。
4. 将下一合法工作恢复为 Issue `#32` 的独立性能基线发现；不在本 Issue 实施测量、预算、优化、目录重新审计或产品迁移。

## 根因与方案

### 根因

- Issue `#35` / PR `#36`、Issue `#34` / PR `#40` 和 Issue `#41` / PR `#42` 已完成，但 `AGENTS.md`、README、Master Plan 与两份报告仍保留旧的进行中描述，造成控制面与 GitHub 权威事实不一致。
- PR `#40` 的 Head 没有在 `main` 因 PR `#42` 前进后自动获得新的 `pull_request` Run；旧检查不能被误作新基线检查，必须以受限方式重新触发。

### 采用方案

采用十路径纯文档 closeout：只更新项目入口、Master Plan、来源报告、排错记录和本 Issue 的四份控制文档。历史 Plan、Session Plan 与 Runtime Workflow 不改写；`upstream/`、`source-lock.json`、Rust、Cargo、测试、Workflow、Ruleset、Release、UI 和 AGOS 均保持零差异。

### 拒绝方案

- 只新增本 Issue 四份文档：拒绝，因为长期入口仍会声称已完成的交付处于待 CI 状态。
- 改写来源 Issue 的历史计划或运行工作流：拒绝，因为会篡改当时的阶段快照。
- 顺带同步缓存、重新审计、性能测量或修复产品：拒绝，因为违反独立 Issue/PR 边界。

## 精确写入范围

以下路径按 Windows `Sort-Object` 的大小写不敏感字典序，以 LF 连接并保留末尾 LF 后计算 SHA-256：

```text
AGENTS.md
docs/plans/2026-07-25-issue-43-v1.2.42-cache-ci-closeout.md
docs/plans/PROJECT-MASTER-PLAN.md
docs/plans/sessions/2026-07-25-issue-43-v1.2.42-cache-ci-closeout.md
docs/reports/2026-07-23-upstream-v1.2.42-sync.md
docs/reports/issue-41-ci-contract-decoupling.md
docs/reports/issue-43-v1.2.42-cache-ci-closeout.md
docs/workflows/2026-07-25-issue-43-v1.2.42-cache-ci-closeout-runtime.md
err.md
README.md
```

```text
scope_hash: sha256:39a26c9301f8ca792498be73117320b1d26c3028fdc1c558703ee7cefb96061e
```

任何新增、删除、重命名路径，或修改 `upstream/`、`source-lock.json`、Rust、Cargo、测试、Workflow、Ruleset、Release、UI、AGOS，都会使本计划失效，必须重新取得项目所有者批准。

## 实施顺序

1. 从 `origin/main` 的 `353391424db5514d022473ba97f601486a190869` 创建隔离工作树和分支，记录 Windows 本机时间与 GitHub Issue/PR/CI 事实。
2. 仅在十路径内新增本 Issue 的计划、Session Plan、Runtime Workflow、报告，并回写长期控制面和两份来源报告。
3. 重算精确路径哈希，核对 GitHub 合并、Issue 关闭和 CI Run 事实；运行 Session Plan、仓库政策、状态字段与空白门禁。
4. 使用普通提交和普通推送创建非 Draft PR；最终 Head 必须重新完成 Review、CI 和对话闭环。
5. 只有项目所有者对该最终 Head 单独授权后，才能使用 Squash Merge；不得 force push，不得删除 `main`。

## 轻量验证

```powershell
# 十路径哈希：UTF-8、LF、末尾 LF
# SHA-256 必须为 39a26c9301f8ca792498be73117320b1d26c3028fdc1c558703ee7cefb96061e

pwsh -NoProfile -File D:\Android_source\ai-growth-os\components\rules\scripts\verify-session-plan.ps1 -Path docs/plans/sessions/2026-07-25-issue-43-v1.2.42-cache-ci-closeout.md
pwsh -NoProfile -File scripts/ci/Verify-RepositoryPolicy.ps1 -RepositoryRoot .
pwsh -NoProfile -File scripts/ci/Verify-ReleaseAuditGate.ps1 -RepositoryRoot .
git diff --check
git diff --cached --check
```

完整 Windows、macOS 与 Linux 构建验证保持交由公开仓库的 GitHub-hosted CI；本 Issue 不在本机运行全量 Rust 编译。

## 停止门

- 精确路径集合或 `scope_hash` 不一致；
- 缓存、产品、测试、Workflow、Ruleset、Release、UI 或 AGOS 出现差异；
- `release_audit` 被错误改写为 `current`，或 `stale-re-audit-required` 被误判为损坏；
- GitHub 合并、CI、Issue 关闭或 PR `#40` 重开事实无法复核；
- 仓库政策、空白门禁、最终 Review、CI 或所有 Review 对话未通过；
- 未获得最终 Squash Merge 的单独所有者授权。

## 外部治理边界

- 本会话的 AGOS Git checkpoint 以 ReportOnly 运行并返回 `GIT_SNAPSHOT_STATUS=ready`；其结果只补充本项目证据。
- Discovery 时默认入口曾返回 `needs-input/unregistered`；该外部缺口按 `AGENTS.md` 记录后绕过，不得阻塞 inputcodex 原生 Issue → 分支 → 验证 → PR → Review/CI → Merge 流程，也不得修改 AGOS。
