# Issue #38：`v1.2.42` 功能目录重新审计 Runtime Workflow

## 工作流元数据

```yaml
workflow_id: inputcodex.issue-38.v1.2.42-catalog-reaudit
task_id: issue-38
baseline_ref: fdb2f98c701800969fc478f95cd2539be598faaa
branch: codex/issue-38-v1.2.42-catalog-reaudit
current_stage: discovery-plan
current_scope_hash: sha256:c7c32b7d07f5f1b04acba9c465e1bc4bc5021228b18c438e85b40d7db5f56add
candidate_implementation_scope_hash: sha256:a384353e947bcb9d95b51ac5ccce49ef9558ca34580c130307a64b6d868819af
implementation_approval_status: pending-owner-decision
merge_policy: squash-only-separate-owner-authorization
```

## 当前允许路径

```text
AGENTS.md
build.md
docs/plans/2026-07-25-issue-38-v1.2.42-catalog-reaudit.md
docs/plans/PROJECT-MASTER-PLAN.md
docs/plans/sessions/2026-07-25-issue-38-v1.2.42-catalog-reaudit.md
docs/reports/issue-38-v1.2.42-catalog-reaudit-discovery.md
docs/workflows/2026-07-25-issue-38-v1.2.42-catalog-reaudit-runtime.md
README.md
```

当前只允许这八条路径发生差异。任何 `parity/`、`upstream/source-lock.json`、Rust、Cargo、缓存、Workflow、Ruleset、Release、UI、性能或 AGOS 差异都必须立即停止。

## 运行节点

### 1. `startup-baseline`

1. 使用 Windows `Get-Date` 记录本机时间；确认 `GIT_AUTHOR_DATE` 与 `GIT_COMMITTER_DATE` 未设置。
2. 确认分支为 `codex/issue-38-v1.2.42-catalog-reaudit`、HEAD 为 `fdb2f98c701800969fc478f95cd2539be598faaa`、工作树在写入前干净。
3. 确认这是 linked worktree，不在 `main` 直接写入。
4. 读取 `AGENTS.md`、`README.md`、`build.md`、`err.md`、Master Plan 与 Issue `#38`。

**输出：** 本机时间、分支、基线、日期覆写变量和干净状态证据。

### 2. `release-freshness-audit`

1. 只读核验最新正式 Release 为 `v1.2.42`，tag commit 为 `657cd33e009ad02515d30db6492cd4e669b06318`。
2. 核验 tree 为 `be938b3cfa7db919c6c17322f4617ab286f280d2`，缓存与 `upstream/source-lock.json` 一致。
3. 核验相对 `v1.2.41` 为 17 个提交、18 个修改文件、0 个新增、0 个删除。
4. 不运行上游代码，不修改只读缓存，不把 `main` 当成功能真源。

**输出：** Release、commit、tree 与 18 文件集合 Fresh 证据；任一漂移都使后续范围失效。

### 3. `prerequisite-closure-check`

核验以下交付已经完成：

- Issue `#34` / PR `#40`：纯缓存同步。
- Issue `#37` / PR `#39`：CRLF CI 合同修复。
- Issue `#41` / PR `#42`：Release Audit 合同解耦。
- Issue `#43` / PR `#44`：长期状态收口；`main` 为 `fdb2f98c701800969fc478f95cd2539be598faaa`，Run `30152001233` 成功。

**输出：** Issue `#38` 不再被硬前置阻塞，但仍等待实施范围批准。

### 4. `delta-classification`

把 18 个修改文件逐条且只归入以下类别：

1. 元数据/版本：Cargo、npm、Tauri 五条。
2. 上游 UI/非功能资产：App、CSS、讨论群二维码三条。
3. 平台路径：`app_paths.rs` 一条。
4. 本地会话：launcher、SQLite、storage、storage tests 四条。
5. Dream Skin companion：类型、测试、renderer、assets、CDP test 五条。

计数必须为 `5 + 3 + 1 + 4 + 5 = 18`。任何文件遗漏、重复或无法解释时停止，不允许用版本号更新掩盖。

### 5. `catalog-impact-map`

只允许形成四个行为复审域：

- `feature.foundation-platform.platform-paths`
- `feature.session-data.local-session-management`
- `feature.plugin-script.dream-skin-library`
- `feature.plugin-script.dream-skin-runtime` 与 `feature.plugin-script.renderer-enhancements`

前两个更新平台与会话语义；library 增加受限 companion 配置；runtime 与 renderer 继续 `exception-pending`。其余 31 个功能只做 Release 对齐，不重写行为。

### 6. `planning-scope-emission`

1. 只写入八条当前允许路径。
2. 使用 Windows `Sort-Object` 的大小写不敏感字典序，以 UTF-8、LF、末尾 LF 计算八路径哈希。
3. 必须得到 `sha256:c7c32b7d07f5f1b04acba9c465e1bc4bc5021228b18c438e85b40d7db5f56add`。
4. 同时输出未来二十六路径候选清单和哈希，但不写入超出八路径的文件。

### 7. `planning-gates`

```powershell
pwsh -NoProfile -File D:\Android_source\ai-growth-os\components\rules\scripts\verify-session-plan.ps1 -Path docs/plans/sessions/2026-07-25-issue-38-v1.2.42-catalog-reaudit.md
pwsh -NoProfile -File scripts/ci/Verify-ReleaseAuditGate.ps1 -RepositoryRoot .
pwsh -NoProfile -File scripts/ci/Verify-RepositoryPolicy.ps1 -RepositoryRoot .
git diff --check
git diff --cached --check
```

Release Audit 在本节点必须继续报告合法的 `stale-re-audit-required`；若已经变成 `current`，说明越过批准门，必须停止并恢复。

### 8. `planning-checkpoint-publish`

1. 核验已跟踪差异、未跟踪文件和暂存路径的并集严格等于八路径。
2. 使用普通提交保存规划 checkpoint；禁止设置 Git 日期覆写变量。
3. 使用普通 SSH 推送；禁止 force push。
4. 不创建 PR。
5. 将 Issue `#38` 移除 `status:blocked`，添加 `status:needs-owner-decision`。
6. 在 Issue 评论回写基线、八路径哈希、18 文件分类、四个行为域、二十六路径哈希和精确批准语句。

### 9. `owner-implementation-scope-stop`

项目所有者明确批准以下值前停止：

```text
批准 Issue #38 二十六路径实施范围与 scope_hash sha256:a384353e947bcb9d95b51ac5ccce49ef9558ca34580c130307a64b6d868819af，允许修改、轻量验证、提交、推送、PR、Review/CI。
```

不得把“继续”“按推荐来”或规划 checkpoint 的批准扩大解释为二十六路径实施授权。最终 Squash Merge 仍需要后续独立授权。

## 批准后的实施节点

以下节点当前不可执行，只在项目所有者批准二十六路径与哈希后启用。

### 10. `catalog-reaudit-execute`

精确允许路径为：

```text
AGENTS.md
build.md
crates/inputcodex-parity/tests/catalog_repository.rs
docs/plans/2026-07-25-issue-38-v1.2.42-catalog-reaudit.md
docs/plans/PROJECT-MASTER-PLAN.md
docs/plans/sessions/2026-07-25-issue-38-v1.2.42-catalog-reaudit.md
docs/reports/issue-38-v1.2.42-catalog-reaudit-discovery.md
docs/workflows/2026-07-25-issue-38-v1.2.42-catalog-reaudit-runtime.md
parity/contracts/foundation-platform.yml
parity/contracts/plugin-script.yml
parity/contracts/provider-network.yml
parity/contracts/remote-install.yml
parity/contracts/session-data.yml
parity/features/foundation-platform.yml
parity/features/plugin-script.yml
parity/features/provider-network.yml
parity/features/remote-install.yml
parity/features/session-data.yml
parity/features/source-index.yml
parity/fixtures/feature.plugin-script.dream-skin-library/baseline.yml
parity/fixtures/feature.plugin-script.dream-skin-library/manifest.yml
parity/fixtures/feature.session-data.local-session-management/baseline.yml
parity/fixtures/feature.session-data.local-session-management/manifest.yml
parity/README.md
README.md
upstream/source-lock.json
```

二十六路径哈希必须为 `sha256:a384353e947bcb9d95b51ac5ccce49ef9558ca34580c130307a64b6d868819af`。

执行顺序：

1. 五域 feature、五域 contract 与 source-index Release 元数据对齐 `v1.2.42`。
2. 更新四个行为复审域的证据、语义、平台结论和例外理由。
3. 更新两组 fixture 的 baseline 与 manifest，不写入真实路径或真实用户数据。
4. 更新 `catalog_repository.rs` 与 Parity README。
5. 最后更新 `source-lock.release_audit` 为 `current`；stale 字段必须按 schema 合法清空。

### 11. `implementation-verification`

本地只运行轻量定向命令：

```powershell
pwsh -NoProfile -File scripts/ci/Verify-ReleaseAuditGate.ps1 -RepositoryRoot .
pwsh -NoProfile -File scripts/ci/Verify-RepositoryPolicy.ps1 -RepositoryRoot .
cargo test -p inputcodex-parity --test catalog_repository --offline
cargo fmt --all -- --check
git diff --check
git diff --cached --check
```

完整 Workspace 与三平台编译测试交由公开仓库标准 GitHub-hosted runners，不使用本机全量 Rust、不使用 self-hosted 或收费 Runner。

### 12. `pr-review-ci`

1. 普通提交与普通推送后创建关联 Issue `#38` 的非 Draft PR。
2. Final Head 必须完成范围、目录语义、fixture 安全和 Release Audit review。
3. 所有 Review 对话必须写明根因、处理和验证证据后解决。
4. GitHub-hosted CI 必须在最终 Head 全绿；不得用旧 Run 代替新 Head 证据。

### 13. `owner-squash-stop`

Review、CI 和全部对话闭环后仍必须停止，等待项目所有者单独授权 Squash Merge。禁止自动合并、Merge Commit、Rebase Merge、force push 或删除 `main`。

## 失败恢复

| 失败条件 | 恢复动作 |
| --- | --- |
| Release、commit、tree 或 18 文件集合漂移 | 丢弃当前候选实施哈希，重新发现和批准；不覆盖既有报告。 |
| 八路径或二十六路径集合变化 | 重算哈希并回到项目所有者批准点。 |
| Release Audit 在规划阶段提前变 current | 恢复未批准目录/锁定差异，重新执行八路径审计。 |
| fixture 含真实路径、真实用户数据或不可复核 base64 | 停止并重新生成合成 fixture，不做就地掩盖。 |
| 行为出现无效功能、有害副作用或错误语义争议 | 建立独立一致性例外 Issue，等待项目所有者决定。 |
| AGOS 不可用、未登记或 needs-input | 记录 bypass，继续项目原生控制面；不修改 AGOS。 |
| 本地 Rust 验证过重 | 保留定向轻量命令，把完整验证交给 GitHub-hosted runners。 |
| Review 或 CI 失败 | 确定根因、修复、重新验证；不得带未解决对话或失败门禁合并。 |
