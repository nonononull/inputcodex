# Issue #43：`v1.2.42` 缓存与 CI 合同状态收口 Runtime Workflow

runtime_workflow_version: inputcodex.runtime-workflow.v1
task_id: issue-43
tracking_issue_ref: https://github.com/nonononull/inputcodex/issues/43
baseline_ref: 353391424db5514d022473ba97f601486a190869
branch: codex/issue-43-v1.2.42-cache-ci-closeout
scope_hash: sha256:39a26c9301f8ca792498be73117320b1d26c3028fdc1c558703ee7cefb96061e
mutation_intent: docs
allowed_operations: exact-ten-path-doc-edit, lightweight-validation, normal-commit, normal-push, non-draft-pr, review-ci
merge_policy: squash-only-with-separate-owner-authorization

## Phase 0：启动基线

1. 使用 Windows `Get-Date` 记录本机时间；不设置 `GIT_AUTHOR_DATE` 或 `GIT_COMMITTER_DATE`。
2. `git fetch origin main --prune`，从 `origin/main` 的 `353391424db5514d022473ba97f601486a190869` 创建隔离工作树。
3. 核对 Issue `#43` 的 Discovery 评论、十路径、`scope_hash` 与项目所有者授权；任何不一致立即停止。
4. 以 ReportOnly 运行 Git snapshot checkpoint。AGOS 只作补充；默认入口 `needs-input/unregistered` 或接口异常时记录后绕过，不修改外部控制面。

## Phase 1：事实收集

1. 读取 `README.md`、`build.md`、`err.md`、Master Plan、`v1.2.42` 同步报告与 Issue `#41` 报告。
2. 从 GitHub 读取 Issue `#34/#41`、PR `#40/#42`、合并提交、Run `30147071062`、`30147559602` 与 `30147841226`。
3. 核对 `upstream/source-lock.json` 为 `v1.2.42`，且 `release_audit.status=stale-re-audit-required`、Issue `#38` 为唯一独立重新审计路径。

## Phase 2：十路径收口写入

1. 新建本 Issue 的计划、Session Plan、Runtime Workflow 和报告。
2. 更新 `AGENTS.md`、README 与 Master Plan，移除已完成交付仍为进行中的状态，不预写本 Issue 自身的动态 merge 事实。
3. 更新两份来源报告和 `err.md`，记录 PR `#40` 未自动重跑的根因、受限关闭/重开与 CI 证据。
4. 禁止修改历史 Plan、Session Plan、Runtime Workflow、`upstream/`、`source-lock.json`、Rust、Cargo、测试、Workflow、Ruleset、Release、UI 或 AGOS。

## Phase 3：轻量验证

```powershell
# 重新计算十路径 UTF-8/LF/末尾 LF SHA-256
pwsh -NoProfile -File D:\Android_source\ai-growth-os\components\rules\scripts\verify-session-plan.ps1 -Path docs/plans/sessions/2026-07-25-issue-43-v1.2.42-cache-ci-closeout.md
pwsh -NoProfile -File scripts/ci/Verify-RepositoryPolicy.ps1 -RepositoryRoot .
pwsh -NoProfile -File scripts/ci/Verify-ReleaseAuditGate.ps1 -RepositoryRoot .
git diff --check
git add -- <approved-ten-paths>
git diff --cached --check
```

必须额外静态核对：路径集合仅十条、所有状态字段不把已完成的 `#34/#40`、`#41/#42` 写成待决，不把 `stale-re-audit-required` 写成失败或 `current`。

## Phase 4：提交与 PR

1. 用 Git 默认 Windows 本机时间创建普通提交；禁止 amend、rebase、force push。
2. 普通推送 `codex/issue-43-v1.2.42-cache-ci-closeout`，创建关联 Issue `#43` 的非 Draft PR。
3. PR 描述写明十路径、`scope_hash`、本地轻量证据、禁止面和“最终 Squash Merge 需单独授权”。

## Phase 5：Review、CI 与收口

1. 最终 Head 后执行两名只读 reviewer 的变更面审查；每条反馈必须写明根因、处理和验证证据，所有对话必须解决。
2. 等待最终 PR Head 的 GitHub-hosted CI 全绿；本机不替代 Windows/macOS/Linux 全量构建。
3. 只有全部门禁通过且项目所有者另行授权后，执行 Squash Merge；禁止 Merge Commit、Rebase Merge、force push 与删除 `main`。
4. 合并后将本 Issue 自身的 PR、Review、CI、Squash 提交与主干 Run 事实回写到 GitHub Issue/PR；若需永久控制面更正，必须新建独立 Issue，不能改写本文件的历史阶段记录。

## 失败恢复

- 路径、哈希或工作树不干净：停止写入，读取真实 Git 状态，撤销本批次未提交的范围外变化后重新核验。
- Session Plan、政策、Release Audit 或空白门禁失败：先按 `err.md` 查重，定位根因；不以文档改写掩盖运行面错误。
- PR 基线推进后没有新的 CI Run：不得把旧 Run 误作新基线结果；只有项目所有者明确授权时才可采用关闭并立即重开 PR 的受限触发方式。
- AGOS 不可用：记录外部缺口后继续 inputcodex 原生流程，绝不修改 AGOS。
